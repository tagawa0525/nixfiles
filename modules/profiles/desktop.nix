# =============================================================================
# Desktop プロファイル
# =============================================================================
# 据え置き・常時通電運用するホスト共通の設定。
# Laptop ホストには適用しない。
# 各 desktop ホストは hosts/<name>/default.nix から本ファイルを import する。
# =============================================================================
{ ... }:

{
  imports = [
    # 他ホストからのリモートビルドを受け付ける（distributed-builds の builder 側）
    ../nix-distributed-builds/builder.nix
  ];
}
